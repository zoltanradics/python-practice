from concat_string import concat_string
from add_prefix import add_prefix
from remove_suffix import remove_suffix

# Add prefix
print('add_prefix: ', add_prefix('auto', 'gram'))
print('add_prefix: ', add_prefix('auto', ['gram', 'motive', 'matic']))

# Remove suffix
print('remove_suffix: ', remove_suffix('ness', 'happiness'))
print('remove_suffix: ', remove_suffix('ness', 'smartness'))